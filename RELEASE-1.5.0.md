版本号递增：versionCode 12 → 13 | versionName 1.4.0 → 1.5.0

群聊功能（1.5.0 完整落地，方案见 docs/群聊方案.txt）

- 首页双 Tab：私聊 / 群聊两个列表，底部按钮 + 滑动联动高亮指示块，
  切换时列表淡入淡出；右上角新建按钮在当前模式下创建对应类型
- 建群流程：群聊模式点新建 → 直接创建空群聊会话（自动编号「新群聊 N」）并进入，
  成员在「会话设置 → 成员管理」中添加；添加时勾选 1～3 个私聊角色，逐个执行
  校验（用户名 / AI 名 / API 测试），通过的正常加入并显示头像，未通过的弹出
  结果弹窗可整体重试或单个配置模型（未配置 API 的角色可直接在该页面新增模型配置）
- 头像点击回复：输入框上方成员头像栏（向右靠齐、随键盘一起动），
  点成员头像该成员接话；最多 1 个在回复 + 2 个排队，头像中央三点/数字 1/2
  显示状态；上下文在点击那一刻定格，队列期间新消息不影响正在进行的回复
- 停止：回复期间发送键变停止键；停止模式 A（只停当前、排队递补）/
  B（清空整个队列，默认）在群聊设置里切换
- 重试：成员回复失败自动重试 5 次并逐次通知（"网络错误，重试中 1/5"），
  任一成员 5 次失败后整个队列取消、头像全部恢复
- 群聊消息展示：每条消息左侧发送者头像 + 名字（成员用角色卡头像与 AI 名，
  用户显示「我」+ 全局头像）；流式带打字动画；气泡操作复用私聊能力
  （用户撤回/改写自己消息，成员消息可重新生成/改写，不打断当前队列）
- 群聊设置：群名称、上下文条数 N（默认 50，范围 1～200）、成员管理
  （查看/添加/移除，最多 3 个最少 1 个，移除后历史消息保留）、停止模式；
  群聊不提供共享用户人设与时间库入口
- 0 成员群聊：保留、历史可查看，顶部提示「请添加成员」点击跳成员管理
- 教程弹窗：首次进入群聊模式列表页弹出（记录已看过只弹一次），
  群聊列表右下角问号按钮随时可再次打开
- 上下文格式统一为「名字：内容」（buildGroupTranscript / toApiMessages）
- 成员回复使用各自私聊的模型配置（模型 / API key / 最大 token 独立）；
  完整级成员带 search_chat 工具检索完整群聊消息；群聊不带私聊记忆
- 群聊小本本：消息达到 40 条阈值自动压缩写入 groupMemory
- 导入导出：v2 导入恢复群聊（会话/消息/成员引用），成员引用悬空的群聊
  计入跳过清单；导出前引用校验保持不变
- 删除保护：被群聊引用的私聊删除前弹确认，确认后该成员从群聊移除、
  历史消息保留；删除群聊走首页多选
- 用户名强制：私聊未设置用户名进入会话先弹窗要求填写，未填写不能发送消息
- 延迟输出：加载动画（头像三点）在整个逐字淡入期间持续，网络快不会提前结束

验证
- :app:testDebugUnitTest 全部单元测试通过（含新增群聊队列 / 规则 / 规划 / 导入测试）
- :app:assembleRelease 构建成功，APK 签名有效
- 覆盖安装保留全部本地数据（conversations.json / messages_*.json 结构兼容：
  新增字段均带默认值）
- 系统要求不变：Android 8.0（API 26）及以上

已知限制
- decideGroupResponder（自然接话判断）未启用：方案定为用户点名回复模式，
  由用户决定下一位发言人
