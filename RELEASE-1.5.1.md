# Quiddity-Android 1.5.1 发布说明

版本号递增：versionCode 13 → 14 | versionName 1.5.0 → 1.5.1

## 本版本内容

- 模型配置：添加/编辑 API 名册时不再单独设置温度，采样温度统一在总设置和会话内调整，对所有模型生效
- 选择框：重做服务商 / 模型选择样式，每个选项用横线分割；服务商名称改用大家熟悉的叫法（如 DeepSeek、Kimi、豆包、通义千问）
- 更新弹窗：更新内容按「新增 / 优化 / 修复」分区展示，支持上下滑动查看，不再是一大段堆在一起

## 验证

- :app:testDebugUnitTest 全部单元测试通过
- :app:assembleDebug 构建成功

## 已知限制

- 已保存的旧模型配置若带温度上限，仍会保留原值用于请求钳制；后续新建/编辑不再写入新的温度设置
