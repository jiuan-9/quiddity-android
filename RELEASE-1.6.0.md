# Quiddity-Android 1.6.0 发布说明

版本号递增：versionCode 14 → 15 | versionName 1.5.1 → 1.6.0

## 本版本内容

本版本将 1.5.1 主线（含 1.5.1 发布后全部 bug 修复）合并进 Agent 模式开发分支（codex/agent-mode-p0），在保留 Agent 模式完整功能的基础上产出 1.6.0。

### Agent 模式（合并自开发分支）

- 主页第三个 Tab「Agent」，默认打开应用停在私聊；Agent 会话列表与私聊/群聊同款组件
- Agent 纯文本聊天页（无气泡、Markdown 保留），会话内设置复用完整汉堡菜单，「AI 人设」改为「选择角色」
- 权限中心设置页：无障碍 / 通知 / 用量 / Shizuku 真实授权状态，教程按系统一条龙（无线调试 / PC 助手）
- 16 个工具：应用列表、读屏、通知、用量、前台应用、应用权限清单、安装时间与来源、后台耗电、流量排行、文件访问能力、截图 + Shizuku 写入类（禁用/启用/AppOps/强停/卸载），危险操作逐次确认 + 白名单 + 审计

### 输入法与角色卡（本版本新增修复）

- 输入法焦点：打开会话内设置（汉堡菜单）、Agent 选择角色等覆盖层时立即收起键盘，不再遮挡页面
- 角色卡唯一 uid：每个私聊的 AI + 用户人设都会同步为角色库中的一张唯一角色卡（characterId），群聊、Agent、小应用统一通过 uid 直接引用，历史私聊启动时自动回填
- 角色删除级联：删除会话时同步移除其绑定的角色卡，仍引用该角色卡的会话（如 Agent）重置为默认人设（无人设），不再残留数据
- Agent 模式隐藏「人设卡导出/导入」：Agent 人设统一来自角色库引用，不再提供单卡导出/导入入口
- Agent「选择角色」改为右侧滑入的 340dp 子面板（与汉堡菜单一致），退出/返回先关闭子面板，不再直接退回会话列表
- 选角色名单按活跃会话驱动：隐藏已删除会话残留的孤儿角色卡；旧数据角色卡未回填引用时按名字匹配补全；仅有人设内容的会话不再漏显示
- 选角色面板退回改为逐级返回：面板 → 会话内设置菜单 → 聊天页 → 会话列表，返回键与右滑手势都只回上一级，不再直接跳回会话列表
- 选角色面板动画按设置面板同款写法重做：半透明遮罩淡入淡出，340dp 侧栏单独从右滑入/滑出（不再整层滑动遮罩）
- 汉堡菜单设置项背景统一为总设置样式：去掉分组卡/展开组描边，圆角与总设置一致，一色透明
- 全局/Agent/汉堡设置页 UI 重写：分区容器保留单一玻璃色（去描边），分区内的每个子设置项不再单独用框圈起来，改为通栏透明行
- 「人设卡」统一改名「角色卡」：导出/导入覆盖「人设」一栏全部设置（AI 人设、用户人设、场景、记忆、快速设定内容）
- 汉堡菜单残余双色项（导出/导入卡片、主动消息权限卡）去框；总设置/Agent/汉堡所有设置大分区改为默认收起、点头部展开（统计分区配色保持不变）
- Agent 设置「工具使用权限」改名「工具使用开关」：全部 13 个工具统一为开关行，写入类（停用/权限修改/强停/卸载）不再依赖 Shizuku 是否授权才显示开关
- 每个工具实时监测所需权限（无障碍/通知/用量/Shizuku），未授权或未配对时开关置灰并在副标题标注「需XX（未开启）」
- 修复 Agent 模式发图片后用户消息显示为空：Agent 消息行渲染与私聊一致的固定「图片」卡片（点击全屏查看）
- 图片卡片抽取为共享组件（ImageMessageCard），私聊/群聊/Agent 统一 UI；群聊发图（选图→识图→发送→卡片渲染）全链路支持
- 深度重构思考模块：思考全程在应用内（本地生成，不依赖厂商 reasoning_content/【思考】标记），思考内容在消息气泡内可展开/收起（默认收起、箭头展开），思考中为 tool 同款滑动高亮，Agent/私聊统一
- Agent 提示词对齐私聊：复用完整 buildSystemPrompt（AI 人设/用户信息/世界场景/记忆/对话方式全部注入），追加 Agent 能力与安全规则，回复风格与私聊一致并抑制无意义 emoji
- 工具 400 彻查修复：工具回填参数空串/非法 JSON 统一补 "{}"（无参数工具不再因 arguments:"" 被拒），超大工具结果截断防第二轮请求过大
- 工具错误处理逻辑：工具报错/拿不到数据时结果明确标注【工具返回异常】并回传模型，AI 承认错误、说明原因并继续回复；开启思考时，工具系列使用后以第一人称评估数据是否正常并给出下一步规划（附着在回复气泡内）
- 本地思考严格受思考开关控制：思考关闭时完全不做思考展示（修复关闭仍显示思考的问题）
- 重写本地思考生成：去除「用户想：…」固定模板，改为按意图（打招呼/提问/查手机信息/闲聊）生成自然多变的第一人称独白，工具系列后思考同样自然化
- API 密钥编辑回显：保存后再次编辑模型/视觉配置时直接显示已保存密钥（可切换隐藏）；API 名册每行新增「默认」勾选，可直接设置全局默认 API
- 新增「全局日志」工具（system_logs）：经 Shizuku 读取 logcat 全局日志（覆盖所有应用），支持条数与关键字过滤；读屏 tool 在无障碍未开启时返回明确指引

## 验证

- :app:testDebugUnitTest 全部单元测试通过
- :app:assembleDebug 构建成功

## 已知限制

- Agent 写入类工具依赖 Shizuku 授权，未授权时对应开关置灰；危险操作每次执行前均需用户确认
- 已保存的旧模型配置若带温度上限，仍会保留原值用于请求钳制；后续新建/编辑不再写入新的温度设置
