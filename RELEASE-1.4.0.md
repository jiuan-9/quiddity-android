# Quiddity-Android v1.4.0 发布说明

> 版本号递增：versionCode 11 → 12 | versionName 1.3.2 → 1.4.0

## 本版本内容

### 新功能

* **对话导出长图**：聊天页长按消息进入多选，选中若干条消息后可一键生成
白底气泡样式的长图，并直接弹出系统分享（微信 / QQ / 保存到相册）
* **聊天记录搜索（全局）**：首页搜索框升级，输入关键词同时搜索会话名称与
所有会话的消息内容，结果按会话分组、按时间倒序；点击结果自动跳到对应消息
* **聊天记录搜索（会话内）**：聊天页顶栏新增搜索图标，可搜索当前会话内的消息，
点击结果滚动定位并短暂高亮该消息

### 上一批未发布改动一并包含

* 更新模块重做：应用内下载失败弹窗引导前往官网；安装前校验包名与签名，
确保覆盖安装与数据保留；「取消下载」真正生效
* API 密钥安全：改用 Android Keystore 设备绑定密钥，旧数据启动时自动迁移

## 验证

* `:app:testDebugUnitTest`：全部单元测试通过（含新增全局搜索 / 长图截断测试）
* `:app:assembleRelease` 构建成功，APK 签名有效（v2 scheme）

## 兼容性

* 覆盖安装保留全部本地数据（conversations.json / messages\_\*.json 结构未变）
* 系统要求不变：Android 8.0（API 26）及以上

